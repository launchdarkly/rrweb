{
  "$schema": "https://turborepo.org/schema.json",
  "tasks": {
    "prepublish": {
      "passThroughEnv": ["PUPPETEER_EXECUTABLE_PATH", "PUPPETEER_HEADLESS", "DISABLE_WORKER_INLINING"],
      "dependsOn": ["^prepublish"],
      "outputs": [
        "lib/**",
        "es/**",
        "dist/**",
        "typings/**",
        ".svelte-kit/**",
        "types/**"
      ]
    },
    "test": {
      "passThroughEnv": ["PUPPETEER_EXECUTABLE_PATH", "PUPPETEER_HEADLESS", "DISABLE_WORKER_INLINING"],
      "dependsOn": ["^prepublish"]
    },
    "test:watch": {
      "persistent": true,
      "cache": false
    },
    "test:update": {
      "passThroughEnv": ["PUPPETEER_EXECUTABLE_PATH", "PUPPETEER_HEADLESS", "DISABLE_WORKER_INLINING"],
      "dependsOn": ["^prepublish"]
    },
    "dev": {
      "dependsOn": ["prepublish", "^prepublish"],
      "persistent": true,
      "cache": false
    },
    "lint": {},
    "check-types": {
      "dependsOn": ["^prepublish"]
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["lib/**", "es/**", "dist/**", "typings/**"]
    }
  }
}
