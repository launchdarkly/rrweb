compressionLevel: mixed

enableGlobalCache: true

# This repo is consumed as a workspace member of launchdarkly/observability-sdk,
# which owns the lockfile. Standalone installs (CI) resolve fresh.
enableImmutableInstalls: false

nodeLinker: node-modules
